---
const { class: className } = Astro.props;
---
<span class={`text-xs font-medium me-2 px-2.5 py-0.5 rounded ${className ?? 'bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-300'}`}>
    <slot/>
</span>